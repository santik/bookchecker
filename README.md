# Spring Boot + VueJS + OPENAI EXAMPLE APP

## Building docker image
 ``docker build .``

To run application correctly 3 environment variables should be submitted:

``OPENAI_KEY, AUTH_USERNAME_HASH, AUTH_PASSWORD_HASH
``



